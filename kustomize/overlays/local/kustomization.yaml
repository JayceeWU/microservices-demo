apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: [../../base]
components: [../../components/network-policies]
patches:
  - target: { kind: Service, name: gatewayservice }
    patch: |-
      - {op: replace, path: /spec/type, value: ClusterIP}
