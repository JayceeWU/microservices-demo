apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shares the local realm with compose.yaml so the two environments cannot drift.
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: keycloak-realm
    files:
      - realm.json=realm.local.json
